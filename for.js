// Contoh penggunaan pengulangan for
for (let i = 0; i < 5; i++) {
    console.log(Iterasi ke-${i});
  }
  
  // Penjelasan:
  // 1. Kata kunci 'for' digunakan untuk membuat pengulangan.
  // 2. Variabel 'i' digunakan sebagai penghitung (counter). Diinisialisasi dengan nilai 0.
  // 3. Ekspresi kondisi 'i < 5' menentukan kapan pengulangan akan berhenti. Jika kondisi ini tidak terpenuhi, pengulangan berhenti.
  // 4. Ekspresi 'i++' akan meningkatkan nilai 'i' setiap kali pengulangan selesai dieksekusi.
  // 5. Pada setiap iterasi, kode di dalam blok kurung kurawal '{ }' akan dijalankan.
  // 6. Hasilnya adalah output iterasi ke-0 hingga ke-4 ke konsol.